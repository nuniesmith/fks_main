
services:
  fks-main:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: nuniesmith/fks:main-latest
    container_name: fks-main
    ports:
      - "8010:8010"
    volumes:
      # Mount central config (read-only)
      - ./config:/app/config:ro
    environment:
      - SERVICE_NAME=fks_main
      - SERVICE_PORT=8010
      - FKS_CONFIG_PATH=/app/config/services/fks_main.yaml
      - MONITOR_URL=http://fks-monitor:8013
      - K8S_NAMESPACE=fks-trading
      - DOMAIN=fkstrading.xyz
      - TLS_ENABLED=false
      - RUST_LOG=info
      # Logs stored inside container at /app/logs (not mounted externally)
      # Access via: docker logs fks-main
    networks:
      - fks-network
    # Note: fks-monitor is defined below, but dependency is optional
    # depends_on:
    #   - fks-monitor
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8010/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
        compress: "true"

  # Note: fks-monitor is managed separately by the monitor service
  # Remove this duplicate definition to avoid container name conflicts

networks:
  fks-network:
    external: true
