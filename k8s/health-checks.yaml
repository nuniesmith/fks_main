# Health check configuration for fks_main
# Use this in your Deployment spec

apiVersion: v1
kind: ConfigMap
metadata:
  name: fks_main-health-config
  namespace: fks-trading
data:
  liveness_probe: |
    httpGet:
      path: /health
      port: 8010
    initialDelaySeconds: 10
    periodSeconds: 30
    timeoutSeconds: 5
    failureThreshold: 3
  
  readiness_probe: |
    httpGet:
      path: /ready
      port: 8010
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 3
    failureThreshold: 3

---
# Example Deployment with health checks
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fks_main
  namespace: fks-trading
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fks_main
  template:
    metadata:
      labels:
        app: fks_main
    spec:
      containers:
      - name: fks_main
        image: nuniesmith/fks_main:latest
        ports:
        - containerPort: 8010
        livenessProbe:
          httpGet:
            path: /health
            port: 8010
          initialDelaySeconds: 10
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 8010
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
