# FKS Trading Platform - Helm Chart Values
# Default configuration for all microservices

# Global settings
global:
  environment: production
  domain: fks-trading.com
  timezone: UTC
  
  # Image registry
  imageRegistry: docker.io
  imagePullPolicy: IfNotPresent
  imagePullSecrets: []
  
  # Storage class
  storageClass: standard
  
  # Resource limits (default - can be overridden per service)
  resources:
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 100m
      memory: 256Mi

# Service-specific configurations
fks_main:
  enabled: true
  name: fks-main
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: main-latest
    pullPolicy: IfNotPresent
  
  # fks_main is a Rust-based orchestration and management service
  # It manages and monitors all services in the platform
  # It runs on port 8010 as per service_registry.json and main.rs
  service:
    type: ClusterIP
    port: 8010
    targetPort: 8010
  
  resources:
    limits:
      cpu: 1000m
      memory: 1536Mi  # Increased from 1Gi to 1.5Gi to resolve memory pressure
    requests:
      cpu: 200m
      memory: 768Mi    # Increased from 512Mi to maintain 2:1 ratio
  
  env:
    # Rust service configuration
    - name: SERVICE_NAME
      value: "fks_main"
    - name: SERVICE_PORT
      value: "8010"
    - name: RUST_LOG
      value: "info"
    - name: RUST_BACKTRACE
      value: "1"
    
    # Monitor integration - fks_main manages and monitors all services
    - name: MONITOR_URL
      value: "http://fks-monitor:8013"
    
    # Kubernetes configuration - for managing K8s resources
    - name: K8S_NAMESPACE
      value: "fks-trading"
    
    # Domain configuration
    - name: DOMAIN
      value: "fkstrading.xyz"
    - name: ALLOWED_HOSTS
      value: "fkstrading.xyz,*.fkstrading.xyz,localhost,127.0.0.1,100.80.141.117"
    
    # Database connection (for monitoring data)
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    
    # Redis connection (for caching and coordination)
    - name: REDIS_HOST
      value: "fks-platform-redis-master"
    - name: REDIS_PORT
      value: "6379"
    - name: REDIS_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-redis
          key: redis-password
    - name: REDIS_URL
      value: "redis://:$(REDIS_PASSWORD)@fks-platform-redis-master:6379/1"
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 10
    targetCPUUtilizationPercentage: 70
    targetMemoryUtilizationPercentage: 80
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /ready

fks_api:
  enabled: true
  name: fks-api
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: api-latest
  
  service:
    type: ClusterIP
    port: 8001
    targetPort: 8001
  
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 256Mi
  
  env:
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    - name: DATA_SERVICE_URL
      value: "http://fks-data:8003"
    - name: AUTH_SERVICE_URL
      value: "http://fks-auth:8009"
    - name: APP_SERVICE_URL
      value: "http://fks-app:8002"
    - name: REDIS_HOST
      value: "fks-platform-redis-master"
    - name: REDIS_PORT
      value: "6379"
    - name: REDIS_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-redis
          key: redis-password
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health

fks_app:
  enabled: true
  name: fks-app
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: app-latest
  
  service:
    type: ClusterIP
    port: 8002
    targetPort: 8002
  
  resources:
    limits:
      cpu: 2000m
      memory: 2Gi
    requests:
      cpu: 500m
      memory: 1Gi
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70

fks_ai:
  enabled: true  # Enabled - GPU with CUDA available
  name: fks-ai
  replicaCount: 1
  image:
    repository: nuniesmith/fks
    tag: ai-latest
  
  service:
    type: ClusterIP
    port: 8007
    targetPort: 8007
  
  # GPU resources with CUDA support
  resources:
    limits:
      cpu: 4000m
      memory: 8Gi
      nvidia.com/gpu: 1  # Request 1 GPU
    requests:
      cpu: 1000m
      memory: 4Gi
      nvidia.com/gpu: 1
  
  # GPU/CUDA environment variables
  env:
    - name: CUDA_VISIBLE_DEVICES
      value: "0"
    - name: NVIDIA_VISIBLE_DEVICES
      value: "all"
    - name: NVIDIA_DRIVER_CAPABILITIES
      value: "compute,utility"
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    - name: DATA_SERVICE_URL
      value: "http://fks-data:8003"
    - name: REDIS_HOST
      value: "fks-platform-redis-master"
    - name: REDIS_PORT
      value: "6379"
    - name: REDIS_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-redis
          key: redis-password
  
  # Note: nodeSelector removed - will schedule on any node with GPU
  # If you need to schedule only on specific GPU nodes, add:
  # nodeSelector:
  #   accelerator: nvidia-tesla-k80  # or your GPU type
  
  # Tolerations for GPU nodes (optional - remove if not needed)
  tolerations:
    - key: nvidia.com/gpu
      operator: Exists
      effect: NoSchedule
  
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 5  # Reduced max replicas for GPU constraints
    targetCPUUtilizationPercentage: 70
    # Custom metrics for AI workload
    customMetrics:
      - type: Pods
        pods:
          metric:
            name: inference_queue_depth
          target:
            type: AverageValue
            averageValue: "100"
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health

fks_data:
  enabled: true
  name: fks-data
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: data-latest
  
  service:
    type: ClusterIP
    port: 8003
    targetPort: 8003
  
  resources:
    limits:
      cpu: 1000m
      memory: 2Gi
    requests:
      cpu: 200m
      memory: 1Gi
  
  env:
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    - name: REDIS_HOST
      value: "fks-platform-redis-master"
    - name: REDIS_PORT
      value: "6379"
    - name: REDIS_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-redis
          key: redis-password
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 4
    targetMemoryUtilizationPercentage: 80
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health

fks_execution:
  enabled: true  # Enabled - will check if binary works, if not will keep disabled
  name: fks-execution
  replicaCount: 1
  image:
    repository: nuniesmith/fks
    tag: execution-latest
  
  # Environment variables for Rust service
  env:
    - name: RUST_LOG
      value: "debug"
    - name: RUST_BACKTRACE
      value: "full"
  
  # Health checks
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 3
  
  service:
    type: ClusterIP
    port: 8004
    targetPort: 8004
  
  # Rust execution engine - vertical scaling
  resources:
    limits:
      cpu: 4000m
      memory: 4Gi
    requests:
      cpu: 500m
      memory: 512Mi
  
  # VPA enabled for execution engine
  verticalPodAutoscaler:
    enabled: false  # Disabled - VPA CRD not installed
    updateMode: Auto
    minAllowed:
      cpu: 500m
      memory: 512Mi
    maxAllowed:
      cpu: 8000m
      memory: 8Gi

fks_ninja:
  enabled: true  # Enabled - Python/Rust API service for NinjaTrader8 communication
  name: fks-ninja
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: ninja-latest
  
  service:
    type: ClusterIP
    port: 8006
    targetPort: 8006
  
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 256Mi
  
  env:
    # NinjaTrader8 connection configuration
    - name: NINJATRADER_HOST
      value: "100.80.141.117"  # Tailscale IP of Windows desktop running NinjaTrader8
    - name: NINJATRADER_PORT
      value: "8080"  # Port that NinjaTrader8 strategy listens on
    - name: NINJATRADER_TIMEOUT
      value: "5"  # Connection timeout in seconds
    
    # Service configuration
    - name: SERVICE_NAME
      value: "fks_ninja"
    - name: SERVICE_PORT
      value: "8006"
    
    # Execution service integration
    - name: EXECUTION_SERVICE_URL
      value: "http://fks-execution:8004"
    
    # Data service integration (for market data validation)
    - name: DATA_SERVICE_URL
      value: "http://fks-data:8003"
    
    # Database connection (for logging signals)
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    
    # Redis connection (for signal queue)
    - name: REDIS_HOST
      value: "fks-platform-redis-master"
    - name: REDIS_PORT
      value: "6379"
    - name: REDIS_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-redis
          key: redis-password
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 4
    targetCPUUtilizationPercentage: 70
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health

fks_mt5:
  enabled: false  # Disabled - no Docker image (C++ MT5 app)
  name: fks-mt5
  replicaCount: 1
  image:
    repository: nuniesmith/fks
    tag: mt5-latest
  
  service:
    type: ClusterIP
    port: 8006
    targetPort: 8006
  
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 256Mi

fks_web:
  enabled: true  # Enabled - hosts the web interface
  name: fks-web
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: web-latest
  
  # Override entrypoint - fks_web uses gunicorn WSGI (no manage.py in this image)
  command:
    - gunicorn
    - "src.django.wsgi:application"
    - "--bind"
    - "0.0.0.0:3001"
    - "--workers"
    - "2"
    - "--chdir"
    - "/app"
  
  service:
    type: ClusterIP
    port: 3001
    targetPort: 3001
  
  resources:
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 200m
      memory: 512Mi
  
  env:
    - name: DJANGO_SETTINGS_MODULE
      value: "src.django.settings"
    - name: ALLOWED_HOSTS
      value: "fkstrading.xyz,*.fkstrading.xyz,localhost,127.0.0.1,100.80.141.117"
    - name: DEBUG
      value: "False"
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    - name: DATABASE_URL
      value: "postgresql://fks_user:$(POSTGRES_PASSWORD)@fks-platform-postgresql:5432/fks_db"
    - name: REDIS_HOST
      value: "fks-platform-redis-master"
    - name: REDIS_PORT
      value: "6379"
    - name: REDIS_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-redis
          key: redis-password
    - name: CELERY_BROKER_URL
      value: "redis://:$(REDIS_PASSWORD)@fks-platform-redis-master:6379/0"
    - name: CELERY_RESULT_BACKEND
      value: "redis://:$(REDIS_PASSWORD)@fks-platform-redis-master:6379/0"
    - name: API_SERVICE_URL
      value: "http://fks-api:8001"
    - name: APP_SERVICE_URL
      value: "http://fks-app:8002"
    - name: DATA_SERVICE_URL
      value: "http://fks-data:8003"
    - name: MAIN_SERVICE_URL
      value: "http://fks-main:8010"
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health

fks_auth:
  enabled: true
  name: fks-auth
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: auth-latest
  
  service:
    type: ClusterIP
    port: 8009
    targetPort: 8009
  
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 256Mi
  
  env:
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    - name: REDIS_HOST
      value: "fks-platform-redis-master"
    - name: REDIS_PORT
      value: "6379"
    - name: REDIS_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-redis
          key: redis-password
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health

fks_portfolio:
  enabled: true
  name: fks-portfolio
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: portfolio-latest
  
  service:
    type: ClusterIP
    port: 8012
    targetPort: 8012
  
  resources:
    limits:
      cpu: 1000m
      memory: 2Gi
    requests:
      cpu: 200m
      memory: 1Gi
  
  env:
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    - name: DATA_SERVICE_URL
      value: "http://fks-data:8003"
    - name: AI_SERVICE_URL
      value: "http://fks-ai:8007"
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health

fks_monitor:
  enabled: true
  name: fks-monitor
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: monitor-latest
  
  service:
    type: ClusterIP
    port: 8013
    targetPort: 8013
  
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 256Mi
  
  env:
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    - name: REDIS_HOST
      value: "fks-platform-redis-master"
    - name: REDIS_PORT
      value: "6379"
    - name: REDIS_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-redis
          key: redis-password
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health

fks_meta:
  enabled: true
  name: fks-meta
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: meta-latest
  
  service:
    type: ClusterIP
    port: 8005
    targetPort: 8005
  
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 100m
      memory: 256Mi
  
  env:
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    - name: EXECUTION_SERVICE_URL
      value: "http://fks-execution:8004"
    - name: DATA_SERVICE_URL
      value: "http://fks-data:8003"
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health

fks_analyze:
  enabled: true
  name: fks-analyze
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: analyze-latest
  
  service:
    type: ClusterIP
    port: 8008
    targetPort: 8008
  
  resources:
    limits:
      cpu: 1000m
      memory: 2Gi
    requests:
      cpu: 200m
      memory: 1Gi
  
  env:
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    - name: DATA_SERVICE_URL
      value: "http://fks-data:8003"
    - name: AI_SERVICE_URL
      value: "http://fks-ai:8007"
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health

fks_training:
  enabled: true
  name: fks-training
  replicaCount: 2
  image:
    repository: nuniesmith/fks
    tag: training-latest
  
  service:
    type: ClusterIP
    port: 8011
    targetPort: 8011
  
  resources:
    limits:
      cpu: 2000m
      memory: 4Gi
    requests:
      cpu: 500m
      memory: 2Gi
  
  env:
    - name: POSTGRES_HOST
      value: "fks-platform-postgresql"
    - name: POSTGRES_PORT
      value: "5432"
    - name: POSTGRES_DB
      value: "fks_db"
    - name: POSTGRES_USER
      value: "fks_user"
    - name: POSTGRES_PASSWORD
      valueFrom:
        secretKeyRef:
          name: fks-platform-postgresql
          key: password
    - name: DATA_SERVICE_URL
      value: "http://fks-data:8003"
    - name: AI_SERVICE_URL
      value: "http://fks-ai:8007"
  
  autoscaling:
    enabled: true
    minReplicas: 2
    maxReplicas: 8
    targetCPUUtilizationPercentage: 70
  
  healthCheck:
    enabled: true
    livenessPath: /health
    readinessPath: /health

# PostgreSQL (TimescaleDB)
postgresql:
  enabled: true
  auth:
    username: fks_user
    password: ""  # Set via secrets
    database: fks_db
  
  primary:
    persistence:
      enabled: true
      size: 50Gi
      storageClass: standard
    
    resources:
      limits:
        cpu: 2000m
        memory: 4Gi
      requests:
        cpu: 500m
        memory: 2Gi
  
  # Enable TimescaleDB extension
  image:
    repository: timescale/timescaledb-ha
    tag: pg14-latest
  
  # Streaming replication for HA
  replication:
    enabled: true
    numSynchronousReplicas: 1
    synchronousCommit: "on"

# Redis
redis:
  enabled: true
  
  # Override image to use latest
  image:
    registry: docker.io
    repository: bitnami/redis
    tag: latest
  
  auth:
    enabled: true
    password: ""  # Set via secrets
  
  master:
    persistence:
      enabled: true
      size: 8Gi
    
    resources:
      limits:
        cpu: 1000m
        memory: 2Gi
      requests:
        cpu: 200m
        memory: 512Mi
  
  # Redis Sentinel for HA
  sentinel:
    enabled: false  # Disabled - sentinel image issue
    quorum: 2

# Ingress configuration
ingress:
  enabled: true
  className: nginx
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/rate-limit: "100"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
  
  hosts:
    - host: fks-trading.com
      paths:
        - path: /
          pathType: Prefix
          service: fks-main
          port: 8000
    
    - host: api.fks-trading.com
      paths:
        - path: /
          pathType: Prefix
          service: fks-api
          port: 8001
    
    - host: web.fks-trading.com
      paths:
        - path: /
          pathType: Prefix
          service: fks-web
          port: 3001
  
  tls:
    - secretName: fks-trading-tls
      hosts:
        - fks-trading.com
        - api.fks-trading.com
        - web.fks-trading.com

# Monitoring
prometheus:
  enabled: true
  server:
    persistentVolume:
      size: 20Gi
    retention: 30d

grafana:
  enabled: true
  adminPassword: ""  # Set via secrets
  persistence:
    enabled: true
    size: 10Gi

# Network policies
networkPolicy:
  enabled: true
  policyTypes:
    - Ingress
    - Egress

# Pod security
podSecurityPolicy:
  enabled: false  # Disabled - PSP deprecated in K8s 1.25+
  runAsNonRoot: true
  allowPrivilegeEscalation: false

# Service mesh (optional)
serviceMesh:
  enabled: false
  provider: istio  # or linkerd

# Backup configuration
backup:
  enabled: true
  schedule: "0 2 * * *"  # Daily at 2 AM
  retention: 30  # Days
  storage:
    size: 100Gi

# Celery worker configuration
celery:
  enabled: false  # Disabled for now - using Celery Beat in fks-main
  replicaCount: 2
  concurrency: 4
  resources:
    limits:
      cpu: 1000m
      memory: 1Gi
    requests:
      cpu: 250m
      memory: 512Mi
