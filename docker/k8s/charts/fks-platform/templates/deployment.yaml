{{- range $service := list "fks_main" "fks_api" "fks_app" "fks_ai" "fks_data" "fks_execution" "fks_ninja" "fks_web" "fks_auth" "fks_portfolio" "fks_monitor" "fks_meta" "fks_analyze" "fks_training" }}
{{- $config := index $.Values $service }}
{{- if $config.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $config.name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    app: {{ $config.name }}
    component: {{ $service }}
    version: {{ $.Chart.AppVersion }}
    managed-by: {{ $.Release.Service }}
spec:
  replicas: {{ $config.replicaCount }}
  selector:
    matchLabels:
      app: {{ $config.name }}
  template:
    metadata:
      labels:
        app: {{ $config.name }}
        component: {{ $service }}
        version: {{ $.Chart.AppVersion }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ $config.service.port | quote }}
        prometheus.io/path: "/metrics"
    spec:
      {{- if $config.nodeSelector }}
      nodeSelector:
        {{- toYaml $config.nodeSelector | nindent 8 }}
      {{- end }}
      
      {{- if $config.tolerations }}
      tolerations:
        {{- toYaml $config.tolerations | nindent 8 }}
      {{- end }}
      
      {{- if $.Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml $.Values.global.imagePullSecrets | nindent 8 }}
      {{- end }}
      
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      
      containers:
      - name: {{ $config.name }}
        image: "{{ $.Values.global.imageRegistry }}/{{ $config.image.repository }}:{{ $config.image.tag }}"
        imagePullPolicy: {{ $config.image.pullPolicy | default $.Values.global.imagePullPolicy }}
        
        {{- if $config.command }}
        command:
          {{- toYaml $config.command | nindent 10 }}
        {{- end }}
        
        ports:
        - name: http
          containerPort: {{ $config.service.targetPort }}
          protocol: TCP
        
        {{- if $config.env }}
        env:
          {{- toYaml $config.env | nindent 10 }}
        {{- end }}
        
        {{- if and (hasKey $config "healthCheck") $config.healthCheck.enabled }}
        livenessProbe:
          httpGet:
            path: {{ $config.healthCheck.livenessPath | default "/health" }}
            port: http
          initialDelaySeconds: {{ $config.healthCheck.initialDelaySeconds | default 30 }}
          periodSeconds: {{ $config.healthCheck.periodSeconds | default 10 }}
          timeoutSeconds: {{ $config.healthCheck.timeoutSeconds | default 5 }}
          failureThreshold: {{ $config.healthCheck.failureThreshold | default 3 }}
        
        readinessProbe:
          httpGet:
            path: {{ $config.healthCheck.readinessPath | default "/health" }}
            port: http
          initialDelaySeconds: {{ $config.healthCheck.initialDelaySeconds | default 10 }}
          periodSeconds: {{ $config.healthCheck.periodSeconds | default 5 }}
          timeoutSeconds: {{ $config.healthCheck.timeoutSeconds | default 3 }}
          failureThreshold: {{ $config.healthCheck.failureThreshold | default 3 }}
        {{- else }}
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        {{- end }}
        
        resources:
          {{- toYaml $config.resources | nindent 10 }}
        
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
        
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: logs
          mountPath: /app/logs
        - name: staticfiles
          mountPath: /app/staticfiles
        {{- if and (hasKey $config "devMounts") $config.devMounts.enabled }}
        {{- range $mount := $config.devMounts.mounts }}
        - name: dev-{{ $mount.name }}
          mountPath: {{ $mount.containerPath }}
          {{- if $mount.readOnly }}
          readOnly: true
          {{- end }}
        {{- end }}
        {{- end }}
      
      volumes:
      - name: tmp
        emptyDir: {}
      - name: logs
        emptyDir: {}
      - name: staticfiles
        emptyDir: {}
      {{- if and (hasKey $config "devMounts") $config.devMounts.enabled }}
      {{- range $mount := $config.devMounts.mounts }}
      - name: dev-{{ $mount.name }}
        {{- if eq $.Values.global.environment "development" }}
        hostPath:
          path: {{ $mount.hostPath }}
          type: DirectoryOrCreate
        {{- else }}
        emptyDir: {}
        {{- end }}
      {{- end }}
      {{- end }}
      
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
{{- end }}
{{- end }}
