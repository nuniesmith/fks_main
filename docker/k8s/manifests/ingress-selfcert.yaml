---
# Ingress for Grafana (with self-signed TLS)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-ingress
  namespace: fks-trading
  labels:
    app: grafana
    component: monitoring
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - grafana.fkstrading.xyz
      secretName: fkstrading-tls  # Using self-signed cert
  rules:
    - host: grafana.fkstrading.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: 3000

---
# Ingress for Prometheus (with self-signed TLS)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus-ingress
  namespace: fks-trading
  labels:
    app: prometheus
    component: monitoring
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    # Optional: Add basic auth for production
    # nginx.ingress.kubernetes.io/auth-type: basic
    # nginx.ingress.kubernetes.io/auth-secret: prometheus-basic-auth
    # nginx.ingress.kubernetes.io/auth-realm: 'Authentication Required'
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - prometheus.fkstrading.xyz
      secretName: fkstrading-tls  # Using self-signed cert
  rules:
    - host: prometheus.fkstrading.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prometheus
                port:
                  number: 9090

---
# Ingress for Alertmanager (with self-signed TLS)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: alertmanager-ingress
  namespace: fks-trading
  labels:
    app: alertmanager
    component: monitoring
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - alertmanager.fkstrading.xyz
      secretName: fkstrading-tls  # Using self-signed cert
  rules:
    - host: alertmanager.fkstrading.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: alertmanager
                port:
                  number: 9093

---
# Ingress for Execution Service (with self-signed TLS)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: execution-ingress
  namespace: fks-trading
  labels:
    app: fks-execution
    component: execution
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/rate-limit: "100"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - execution.fkstrading.xyz
      secretName: fkstrading-tls  # Using self-signed cert
  rules:
    - host: execution.fkstrading.xyz
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fks-execution
                port:
                  number: 8000

---
# NOTE: For production with Let's Encrypt, uncomment below and install cert-manager

# ---
# # ClusterIssuer for Let's Encrypt (production)
# apiVersion: cert-manager.io/v1
# kind: ClusterIssuer
# metadata:
#   name: letsencrypt-prod
# spec:
#   acme:
#     server: https://acme-v02.api.letsencrypt.org/directory
#     email: admin@fks-trading.com  # CHANGE THIS
#     privateKeySecretRef:
#       name: letsencrypt-prod
#     solvers:
#       - http01:
#           ingress:
#             class: nginx

# Then update ingress annotations to:
# cert-manager.io/cluster-issuer: "letsencrypt-prod"
# And change secretName to: grafana-tls, prometheus-tls, etc.
