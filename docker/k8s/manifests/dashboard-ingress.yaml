---
# Kubernetes Dashboard Ingress
# Exposes the dashboard via ingress for domain access

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kubernetes-dashboard-ingress
  namespace: kubernetes-dashboard
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
    nginx.ingress.kubernetes.io/rewrite-target: /
    # Note: configuration-snippet is disabled by ingress administrator
    # Using backend-protocol HTTPS instead for SSL verification
spec:
  ingressClassName: nginx
  rules:
  - host: dashboard.fkstrading.xyz
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kubernetes-dashboard
            port:
              number: 443
  tls:
  - hosts:
    - dashboard.fkstrading.xyz
    secretName: dashboard-tls

