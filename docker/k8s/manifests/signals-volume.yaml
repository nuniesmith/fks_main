---
# Signals Volume Configuration
# DISABLED: Using emptyDir in pod volumes instead to avoid creating directory on host
# If persistent storage is needed later, use a proper storage class (not hostPath)
#
# apiVersion: v1
# kind: PersistentVolume
# metadata:
#   name: signals-pv
#   namespace: fks-trading
# spec:
#   capacity:
#     storage: 10Gi
#   accessModes:
#     - ReadWriteMany
#   persistentVolumeReclaimPolicy: Retain
#   storageClassName: local-storage
#   hostPath:
#     path: /home/jordan/Nextcloud/code/repos/fks/signals
#     type: DirectoryOrCreate
# ---
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: signals-pvc
#   namespace: fks-trading
# spec:
#   accessModes:
#     - ReadWriteMany
#   resources:
#     requests:
#       storage: 10Gi
#   storageClassName: local-storage
---
# Alternative: ConfigMap for signals directory path (if using hostPath directly)
apiVersion: v1
kind: ConfigMap
metadata:
  name: signals-config
  namespace: fks-trading
data:
  SIGNALS_DIR: "/app/signals"
  # SIGNALS_HOST_PATH removed - using emptyDir volume instead
